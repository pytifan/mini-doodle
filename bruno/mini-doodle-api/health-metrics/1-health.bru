meta {
  name: 1. Health Check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/actuator/health
  body: none
  auth: none
}

tests {
  test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });
  test("Status is UP", () => {
    expect(res.body.status).to.equal("UP");
  });
}
