meta {
  name: 3. OpenAPI Spec
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/v3/api-docs
  body: none
  auth: none
}

tests {
  test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });
  test("Has paths", () => {
    expect(res.body.paths).to.exist;
  });
}
