meta {
  name: 1. Delete Slot (free, no meeting)
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/api/v1/users/{{userId}}/slots/{{slotId}}
  body: none
  auth: none
}

tests {
  test("Status is 204", () => {
    expect(res.status).to.equal(204);
  });
}
