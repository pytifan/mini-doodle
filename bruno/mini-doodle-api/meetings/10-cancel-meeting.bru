meta {
  name: 10. Cancel Meeting
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/v1/users/{{userId}}/meetings/{{meetingId}}
  body: none
  auth: none
}

tests {
  test("Status is 204", () => {
    expect(res.status).to.equal(204);
  });
}
