meta {
  name: 3. Book Meeting - Bad Participant (404)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/users/{{userId}}/slots/{{slot3Id}}/meetings
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "title": "Ghost Meeting",
    "participantIds": [9999]
  }
}

tests {
  test("Status is 404", () => {
    expect(res.status).to.equal(404);
  });
}
