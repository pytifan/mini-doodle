meta {
  name: 8. Cannot Delete Slot with Meeting (409)
  type: http
  seq: 8
}

delete {
  url: {{baseUrl}}/api/v1/users/{{userId}}/slots/{{slot2Id}}
  body: none
  auth: none
}

tests {
  test("Status is 409", () => {
    expect(res.status).to.equal(409);
  });
}
