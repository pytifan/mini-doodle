meta {
  name: 6. List User Meetings
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/api/v1/users/{{userId}}/meetings
  body: none
  auth: none
}

tests {
  test("Status is 200", () => {
    expect(res.status).to.equal(200);
  });
  test("Has content", () => {
    expect(res.body.content).to.be.an("array");
  });
}
