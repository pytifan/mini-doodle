meta {
  name: 2. Availability - Missing Params (400)
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/users/{{userId}}/availability
  body: none
  auth: none
}

tests {
  test("Status is 400", () => {
    expect(res.status).to.equal(400);
  });
}
